'use strict';

describe('Directive: templateCanvas', function () {

  // load the directive's module
  beforeEach(module('freshcardUiApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

  it('should render a template canvas', inject(function ($compile) {
  }));
});
